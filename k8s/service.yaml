apiVersion: v1
kind: Service
metadata:
  name: vmpm-service
spec:
  type: NodePort
  selector:
    app: vmpm
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7000
      nodePort: 30010